# AGENTS.md

## Project

This is a .NET library. The library is in `src/`. Generated output in `bin/` and `obj/`; never edit those directly.

## Commands

- Build: `dotnet build`
- Run: `dotnet run`
- Test: `dotnet run`

## Commit Message Rules

- Use Conventional Commits
- Use past-tense commit messages
- use verbs like: added, updated, fixed, improved
- Max 72 chars in subject
- Wrap body at 100 chars
